<div class="text-center">
<h1>Liste des Items</h1>
</div>
<div class="container">
<div class="row">
<% @items.each do |item| %>
<div class="col-12">
<div class="shadow bg-white rounded p-4 d-flex justify-content-between">
<h2><%= item.thing %></h2>
<p><b><%= item.created_at.strftime("%D") %></b></p>
</div>
</div>
<% end %>
<%= link_to "Ajouter item", new_item_path %>
<%= link_to "Retour à la liste des activités", activities_path, method: :get %>


</div>
</div>
